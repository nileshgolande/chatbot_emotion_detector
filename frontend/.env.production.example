# Copy to `.env.production` before `npm run build` on the server (or set in CI).
# Public API must match the same origin you serve in nginx (/api → Daphne).
REACT_APP_API_URL=https://www.myemotionaichatbot.duckdns.org
