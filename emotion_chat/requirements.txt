# Emotion Chat backend — pip install -r requirements.txt (from emotion_chat/).
# Python 3.11+ recommended.

# --- Django & REST ---
Django>=5.0,<6
djangorestframework>=3.15,<4
djangorestframework-simplejwt>=5.3,<6
django-cors-headers>=4.3,<5
python-decouple>=3.8
python-dotenv>=1.0

# --- Database (PostgreSQL + pgvector ORM integration) ---
psycopg2-binary>=2.9,<3
pgvector>=0.3

# --- HTTP client (Groq / OpenRouter / Gemini REST) ---
requests>=2.31,<3

# --- ASGI, WebSockets, cache, Celery ---
daphne>=4.0,<5
channels>=4.0,<5
channels-redis>=4.1,<5
redis>=5.0,<6
django-redis>=5.4,<6
celery[redis]>=5.3,<6

# --- LangGraph chat pipeline ---
langgraph>=0.2.40
langchain-core>=0.3.0

# --- Emotion classifier & sentence embeddings (CPU ok; GPU optional) ---
numpy>=1.26,<3
torch>=2.2,<3
transformers>=4.40,<5
sentence-transformers>=3.0,<4
safetensors>=0.4
huggingface_hub>=0.23
Pillow>=10.0,<12

# --- Optional: uncomment for local testing ---
# pytest>=8.0
# pytest-django>=4.8
