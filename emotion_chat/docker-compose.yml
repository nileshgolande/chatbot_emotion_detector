services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: emotion
      POSTGRES_PASSWORD: emotion
      POSTGRES_DB: emotion_chat
    ports:
      - "5432:5432"
    volumes:
      - pgdata_emotion:/var/lib/postgresql/data

volumes:
  pgdata_emotion:
